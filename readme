Threat_Map

A script that checks ssh connections for ip adresses and prints it on a map / database

CREATE TABLE IF NOT EXISTS data (id INT AUTO_INCREMENT PRIMARY KEY,date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,ip Text,city Text,region Text,country Text,lat_long Text)
